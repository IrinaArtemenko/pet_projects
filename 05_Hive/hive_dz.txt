Hive 
 
ѕредставьте, что подготовлены требовани€ на заполнение таблицы 
следующей структуры: id bigint, col1 string, col2 string, col3 string. 
ѕри этом таблица должны быть партиционирована по полю p int. 
‘ормат файлов - parquet.  
1)  —оздайте таблицу в hive с указанной выше структурой. 
2)  —оздайте тестовые данные в таблице: 10 партиций от 1 до 10, с 1000 
    строчками в каждой. ѕоле id заполн€етс€ любым _уникальным_ 
    числом, пол€ col1, col2, col3 - 3 рандомных алфавитно-цифровых 
    значени€ (f9x, xcf, 123 и т.д.). ѕодсказка: дл€ генерации тестовых 
    данных можно использовать конструкцию lateral view posexplode, 
    встроенные функции rand(), chr(). 
3)  —делайте несколько выборок: дл€ каждой патриции выведите 
    минимальное, максимальное значение id и общее количество строк дл€ 
    случаев когда 
    1)  все три текстовых пол€ заполнены буквами 
    2)  все три текстовых пол€ заполнены цифрами 
 
ѕредставьте, что через некоторое врем€ служба безопасности 
проверила нашу таблицу и потребовала удалить из таблицы поле 
col2, так как оно не удовлетвор€ет правилам безопасности. 

4)  ”далите из таблицы поле col2, при этом данные по остальным пол€м в 
    таблице должны быть сохранены. ѕодсказка: напр€мую поле из 
    таблицы удалить не получитс€, нужен р€д последовательных действий. 